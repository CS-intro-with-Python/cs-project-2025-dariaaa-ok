name: Tests

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run unit tests
        run: |
          pip install -r requirements.txt
          pytest tests/unit

      - name: Start services
        run: docker compose up -d

      - name: Wait for services
        run: sleep 10

      - name: Run integration tests
        run: docker compose exec -T web pytest tests/integration

      - name: Stop services
        run: docker compose down
